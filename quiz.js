/*
Object representing a quiz and related functions
------
*/

// Load from data

// Has an array of questions?

// Has a indexOfDisplayedQuestion?

// showCurrentQuestion function

// show previous function

/*Quiz.prototype.selectionIsValid = function() {
    if($('input[name=q]:checked').val()){
        return true;
    } else {
        return false;
    }
};*/

// calcuate score function

// show quiz results